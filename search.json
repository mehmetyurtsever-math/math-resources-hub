---
layout: null
permalink: /search.json
---
[
  {% assign first = true %}
  {% for p in site.pages %}
    {% if p.title and p.layout == 'default' and p.url != '/search/' %}
      {% unless first %},{% endunless %}
      {% assign first = false %}
      {
        "title": {{ p.title | jsonify }},
        "url": {{ p.url | prepend: site.baseurl | jsonify }},
        "content": {{ p.content | strip_html | strip_newlines | jsonify }}
      }
    {% endif %}
  {% endfor %}
]
